# Required on all platforms
NODE_ENV=production

# Optional: log verbosity (debug | info | warn | error)
LOG_LEVEL=info

# Set automatically by Replit, Render, and most platforms.
# Set manually if self-hosting.
PORT=8080
